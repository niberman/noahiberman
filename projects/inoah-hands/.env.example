# API key for /click, /type, /key, /video_feed (must match VITE_AGENT_SECRET in the web app).
# Required: the service refuses to start without it. Generate with `openssl rand -hex 32`.
AGENT_SECRET_KEY=
# Comma-separated browser origins allowed to call this service (CORS).
AGENT_ALLOWED_ORIGINS=http://localhost:8080
