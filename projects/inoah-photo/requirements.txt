# iNoah Photo Dependencies
fastapi>=0.100.0
uvicorn>=0.22.0
Pillow>=9.0.0
python-multipart>=0.0.6

# Note: Also requires inoah-core to be installed
# pip install -e ../inoah-core

# Note: FaceFusion must be installed separately in ./facefusion/
